{
  "build": {
    "builder": "Nixpacks",
    "buildCommand": "pip install -r BeHazeld_OS_Backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn BeHazeld_OS_Backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "healthCheckPath": "/health"
  }
}
